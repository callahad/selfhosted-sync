# vim: set filetype=dosini :

[server:main]
use = egg:gunicorn
host = 127.0.0.1
port = 5000
workers = 2
timeout = 60

[app:main]
use = egg:syncserver

[syncserver]
public_url = http://{{ server_hostname }}/
secret = {{ secret | default(generated_secret.stdout) }}
allow_new_users = true
